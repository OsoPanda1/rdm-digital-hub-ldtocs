# scripts/json-schemas

Esquemas JSON (JSON Schema / zod) que alimentan la generación de parsers tipo `turbo-json-parse`.

- Contratos de Isabella (`domains/ai/contracts/**`)
- Esquemas de mapas (`domains/maps/schemas/**`)
- Esquemas de digital twins (`domains/digital-twins/schemas/**`)
- Registros de negocios, rutas y eventos del territorio

Estado: **scaffold** — carpeta creada.
