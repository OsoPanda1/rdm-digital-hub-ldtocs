# data/raw-json

JSON crudo de ingesta (streaming estilo `turbo-json`): registros de negocios, rutas turísticas, capas de mapas y digital twins.

Procesamiento: `json:stream-merge` → `data/combined-json`.

Estado: **scaffold** — carpeta creada.
