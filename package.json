{
  "name": "nodo-cero-rdm",
  "private": true,
  "devDependencies": {
    "turbo": "^2.10.7",
    "typescript": "^5.0.0",
    "pnpm": "^11.18.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "domains/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  }
}
