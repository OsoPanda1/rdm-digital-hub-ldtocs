{
  "name": "nodo-cero-rdm",
  "private": true,
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@10.28.0",
  "devDependencies": {
    "turbo": "^1.10.0",
    "typescript": "^5.0.0",
    "pnpm": "^8.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "domains/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  }
}
