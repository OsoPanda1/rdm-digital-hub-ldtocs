{
  "name": "nodo-cero-rdm",
  "private": true,
  "devDependencies": {
    "turbo": "^2.9.14",
    "typescript": "^5.0.0",
    "pnpm": "^10.34.4"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "domains/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  }
}
