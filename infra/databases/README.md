# infra-databases

Capa de orquestación multi-base de datos del Nodo Cero: Supabase/Postgres, Redis, Neon y otras OSS gratuitas.

| Comando | Estado |
| --- | --- |
| `migrate` | delegado a `scripts/apply-migrations.ps1` |
| `seed` | delegado a `scripts/apply-seed.ps1` |
| `db:check` | pendiente |

Estado: **scaffold** — estructura creada.
