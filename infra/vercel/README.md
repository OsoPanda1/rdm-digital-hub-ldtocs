# Vercel onboarding

Contiene instrucciones específicas para deploy en Vercel, naming de secrets y recomendaciones de regiones.

- Añade los secrets listados en `.env.example` como Vercel Project Environment Variables
- Configura las regions en vercel.json
- Revisa `functions` y `crons` en vercel.json
